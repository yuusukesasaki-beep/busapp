# ODPT アクセストークン(https://developer.odpt.org/ で無料登録して取得)
#
# このファイルをコピーして .env を作り、値を入れる:  cp .env.example .env
# .env は .gitignore 済み。本番(GitHub Actions)は Secrets の ODPT_TOKEN を使う。
# 絶対にコード・ログ・コミットに含めないこと(設計書 §6)。
ODPT_TOKEN=
